run = "cd web && gunicorn app:app --bind 0.0.0.0:8080 --workers 2 --timeout 120 --log-level info"

[deployment]
run = "cd web && gunicorn app:app --bind 0.0.0.0:8080 --workers 2 --timeout 120 --log-level info"

[[ports]]
localPort = 8080
externalPort = 80
